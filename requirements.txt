# ===== 期末專案：台股 AI 量化分析平台 =====
# 套件版本鎖定，確保 Streamlit Cloud 部署可重現

# ---- Web 框架 ----
streamlit==1.40.1
plotly==5.24.1

# ---- 資料來源 ----
# yfinance 用 >= 因為 Yahoo API 常變動，舊版會失效
yfinance>=0.2.55,<0.3
twstock==1.3.1
requests>=2.32,<3
curl_cffi>=0.7.0  # yfinance 新版需要，模擬瀏覽器避免被擋

# ---- 資料處理 ----
pandas==2.2.3
numpy==1.26.4

# ---- 技術指標 (使用純 pandas 自行實作，不依賴 TA-Lib / pandas-ta) ----

# ---- 機器學習 / 深度學習 ----
scikit-learn==1.5.2
torch==2.5.1
# torch CPU 版本即可，Streamlit Cloud 沒 GPU

# ---- 工具 ----
joblib==1.4.2
python-dateutil==2.9.0
